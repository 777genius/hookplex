# {{.ProjectName}}

Codex project scaffold generated by [hookplex](https://github.com/hookplex/hookplex) `init`.

## Build

```bash
go mod edit -replace=github.com/hookplex/hookplex/sdk=<absolute-path-to>/sdk/hookplex
go test ./...
go build -o bin/{{.ProjectName}} ./cmd/{{.ProjectName}}
./bin/{{.ProjectName}} notify '{"client":"codex-tui"}'
```

## Codex

1. Put repository instructions in `AGENTS.md`.
2. Keep project-local Codex settings in `.codex/config.toml`.
3. The generated binary handles the documented `notify` hook.
