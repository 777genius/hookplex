# {{.ProjectName}}

Codex project instructions:

- The generated entrypoint is `{{.Entrypoint}}`.
- It handles the stable Codex `notify` path wired through `.codex/config.toml`.
- Use `{{.Entrypoint}} notify '{"client":"codex-tui"}'` as the local smoke command.
- Put repository-specific operating instructions here.
- Keep hook responses on stdout and diagnostics on stderr.
