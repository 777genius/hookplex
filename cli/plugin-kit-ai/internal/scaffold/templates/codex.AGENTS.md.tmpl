# {{.ProjectName}}

Codex project instructions:

- Build the helper binary with `go build -o bin/{{.ProjectName}} ./cmd/{{.ProjectName}}`.
- The binary handles the stable Codex `notify` path wired through `.codex/config.toml`.
- Use `{{.Entrypoint}} notify '{"client":"codex-tui"}'` as the local smoke command.
- Put repository-specific operating instructions here.
- Keep wire output on stdout and diagnostics on stderr.
