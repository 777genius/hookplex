# {{.ProjectName}}

Generated by `plugin-kit-ai init`.

- Target lane: `gemini`
- Platform family: `extension_package`
- Launcher contract: `optional`
- Runtime claim: `runtime-supported beta extension target`
- Runtime mode: `go`

## Quick Start

```bash
go mod tidy
go test ./...
plugin-kit-ai render .
plugin-kit-ai render --check .
plugin-kit-ai validate . --platform gemini --strict
gemini extensions link .
```

## Files

- `launcher.yaml`: Go entrypoint metadata for the Gemini beta runtime lane
- `cmd/{{.ProjectName}}/main.go`: typed Go hook registrar starter
- `targets/gemini/package.yaml`: Gemini extension metadata
- `targets/gemini/contexts/GEMINI.md`: primary authored Gemini context
- `targets/gemini/hooks/hooks.json`: managed Gemini hook wiring for `SessionStart`, `SessionEnd`, `BeforeTool`, and `AfterTool`
- `gemini-extension.json`: rendered native extension manifest

## Notes

- This lane is `public-beta`, not production-ready.
- `plugin-kit-ai test` and `plugin-kit-ai dev` stay focused on the stable Claude/Codex runtime fixture lanes. For Gemini beta, use `go test ./...`, `plugin-kit-ai render --check .`, `plugin-kit-ai validate . --platform gemini --strict`, then exercise the extension through `gemini extensions link .` and a real Gemini CLI session.
- Keep Gemini-specific authored assets inside `targets/gemini/...`.
- Keep stdout reserved for Gemini hook JSON responses and write diagnostics to stderr only.
