# {{.ProjectName}}

Codex project scaffold generated by [plugin-kit-ai](https://github.com/plugin-kit-ai/plugin-kit-ai) `init`.

## Status

- Runtime: `go`
- Status: `production-ready`, stable default path
- Bootstrap contract: Go `1.22+`

Choose this lane when long-term support, packaged distribution, and the clearest release story matter more than matching an existing interpreted runtime.

## First Run

```bash
go mod edit -replace=github.com/plugin-kit-ai/plugin-kit-ai/sdk=<absolute-path-to>/sdk/plugin-kit-ai
go test ./...
go build -o bin/{{.ProjectName}} ./cmd/{{.ProjectName}}
plugin-kit-ai validate . --platform codex --strict
{{.Entrypoint}} notify '{"client":"codex-tui"}'
```

## Stable Default

- `Notify`

The scaffold wires the documented stable Codex `notify` path.

## Target Files

- `AGENTS.md`: repository instructions for Codex
- `targets/codex/package.yaml`: authored Codex package metadata
- `.codex/config.toml`: rendered managed Codex config

Keep stdout reserved for Codex responses and write diagnostics to stderr only.
