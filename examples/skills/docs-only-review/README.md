# docs-only-review

This example demonstrates an instruction-only skill.

Use it when you want to share a reusable review procedure but do not need a command at all.
The checklist is intentionally concrete so the example reads like a real maintainer workflow rather than a placeholder.
