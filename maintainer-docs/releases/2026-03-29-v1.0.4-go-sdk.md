# `v1.0.4` Release Notes

## Summary

- release tag: `v1.0.4`
- SDK module tag: `sdk/v1.0.4`
- candidate commit SHA: `6d0bc9d27e51b67e5945f2c362e4f05662b6e185`
- release date: `2026-03-29`
- release type: `stable patch`
- status: `stable-approved`

## What Changed

- moved the Go SDK module root from `sdk/plugin-kit-ai/` to `sdk/` so the public module path `github.com/777genius/plugin-kit-ai/sdk` now matches the physical repo layout
- replaced public placeholder SDK versions with `v1.0.4` in Go starters, Go production examples, and Go scaffold templates
- removed newcomer-facing `go mod edit -replace=github.com/777genius/plugin-kit-ai/sdk=...` guidance from public starter and scaffold docs
- re-synced the external Go starter templates after the structural move and README cleanup
- kept `v1.0.3` published but explicitly treated it as known-bad for Go SDK module consumption

## Evidence

- required:
  - release-commit `Required` run `23718095965` failed on a hardcoded `/opt/homebrew/bin/go` assumption
  - immediate post-release portability fix landed on `main` as `71540da`
  - follow-up `Required` run `23718172853` passed
- install-compat:
  - not separately re-run on the `v1.0.4` tag because this patch did not change the install contract surfaces
- polyglot-smoke:
  - not separately re-run on the `v1.0.4` tag because this patch was limited to Go SDK module layout and Go consumer surfaces
- generated-config/runtime-contract drift:
  - no additional drift issue was introduced by the SDK root move; starter/template sync and runtime-contract guards stayed aligned in the corrective wave
- extended:
  - not separately re-run for the tag; no extended-only surface changed in this patch
- live:
  - not separately re-run for the tag; release publish channels were verified directly via release workflows below
- Go module proxy:
  - `go list -m github.com/777genius/plugin-kit-ai/sdk@v1.0.4` succeeded from a clean temp module
  - `go get github.com/777genius/plugin-kit-ai/sdk@v1.0.4` succeeded from a clean temp module
- Homebrew tap: success in release workflow run `23718142304`
- npm publish: success in release workflow run `23718142319`
- PyPI publish: GitHub-side Trusted Publishing wiring is now enabled, but manual publish run `23738158283` failed with PyPI `invalid-publisher` because the matching trusted publisher has not yet been registered on the PyPI side

## Known Limitations

- `v1.0.3` remains a known-bad release for Go SDK normal-module consumption and should not be used for `github.com/777genius/plugin-kit-ai/sdk`.
- PyPI still needs a matching trusted publisher configured on the PyPI side for:
  - owner/repo: `777genius/plugin-kit-ai`
  - workflow: `.github/workflows/pypi-publish.yml`
  - environment: `pypi`
